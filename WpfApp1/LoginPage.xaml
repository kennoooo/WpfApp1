<Page x:Class="WpfApp1.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Авторизация" Height="450" Width="800">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel Width="300">
            <TextBlock Text="Вход в систему" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Логин"/>
            <TextBox x:Name="TxtLogin" Margin="0,5,0,15" Padding="5"/>

            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="TxtPass" Margin="0,5,0,20" Padding="5"/>

            <Button Content="Войти" Click="BtnLogin_Click" Height="35" Margin="0,0,0,10"/>

            <Button Content="Регистрация" Click="BtnToReg_Click" Background="Transparent" BorderBrush="Transparent" Foreground="Blue" Cursor="Hand"/>
            <Button Content="Назад на главную" Click="BtnBack_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>